import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#222",
  },
  chatList: {
    flex: 1,
    color: "#fff",
  },
  matchesList: {
    maxHeight: 180,
    color: "#fff",
  },
  matchesTitle: {
    color: '#fff',
    fontSize: 16,
    textAlign: 'center'
  }
});
